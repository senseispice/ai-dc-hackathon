
  # UI-Hackathon-DC

  This is a code bundle for UI-Hackathon-DC. The original project is available at https://www.figma.com/design/GaNrwag76sXIO3KbI8lV9r/UI-Hackathon-DC.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  